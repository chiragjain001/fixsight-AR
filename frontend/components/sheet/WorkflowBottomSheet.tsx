/**
 * WorkflowBottomSheet — DEPRECATED.
 *
 * This component has been replaced by the new progressive-disclosure
 * workflow: ContextCard (State 5) + ResponsePlan (State 6).
 *
 * Kept here for reference but no longer rendered in the app.
 */
import React from 'react';
import { View } from 'react-native';

export function WorkflowBottomSheet() {
  return null;
}
